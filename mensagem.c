/* Este programa é primeiro exemplo em C */
/* Esta é uma bibilioteca padrão para entrada e saida de dados 
   e está incluindo neste programa */
#include<stdio.h>
#include<stdlib.h>//Essa bibilioteca é para comanda system("puse")
// programa principal (estrutura em C)
int main(){
	//O comando printf("...") é para mostrar uma mensagem na saida
	// \n para pular uma linha (do cursor)
	printf("Seja bem vindo a linguagem C !\n");
	
	system("pause"); //Parar o resultado na saida
	return 0; //fim do programa
}
